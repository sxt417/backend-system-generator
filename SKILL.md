---
name: backend-system-generator
description: 生成、改造或审核成熟的 SaaS 与数据后台系统，适合经营仪表盘、数据分析、内容管理、用户与权限、订单与资源、任务队列、排期日历、人员管理和配置中心等场景。支持圆角分组或堆叠柱、平滑折线、环形 KPI、分段环图、容量进度条、月历、时间线、AI 洞察及兼容扩展图表。默认生成浅色单主题；仅在用户明确要求深色，或输入数据包含深色模式按钮或激活状态时，生成深色单主题，并优先采用数据提供的对应色号。在用户要求“成熟后台”、“低焦虑高密度”、“专业科技感”、“数据密集”、“多页面视觉一致”，或需要新建、重构、统一 Dashboard、Detail、Scheduling、Queue、Resource、Analytics、Settings 等后台页面时使用。也可用于审核 HTML、React、Vue、Tailwind 或设计稿是否符合本设计系统。
---

# Backend System Generator

把参考图的视觉语言转译为可执行的后台设计系统。始终追求“高信息密度，零视觉恐慌”，并让所有页面像同一个成熟产品，而不是一组互不相关的 Demo。每次生成只采用一个全局主题。

## 启动必读

执行任何设计或实现前，必须：

1. 完整读取 [references/design-system.md](references/design-system.md)，获取视觉规范与设计 Token。
2. 根据页面类型读取 [references/page-templates.md](references/page-templates.md)，选用且保持模板骨架。
3. 页面包含图表、日历、时间线、容量进度或数据洞察时，完整读取 [references/chart-design-system.md](references/chart-design-system.md)，先选择表达方式再实现。
4. 交付前完整读取 [references/qa-checklist.md](references/qa-checklist.md)，逐项验收。
5. 需要核对原始气质时，查看 [assets/reference.webp](assets/reference.webp)；需要核对主蓝渐变与 Plus Jakarta Sans 字体时，查看 [assets/theme-references](assets/theme-references)；需要核对图表风格时，查看 [assets/chart-references](assets/chart-references)。
6. 需要落地前端时，复用 [assets/calm-admin-tokens.css](assets/calm-admin-tokens.css)，不要重新发明另一套变量。

## 核心原则

强制遵守以下原则：

- 当输入包含现有页面、截图、设计稿、代码或内容清单，且任务是换风格、重构或高保真还原时，锁定原有内容组织结构并保持 100% 信息完整。不得为了视觉简洁、版式对称、响应式适配或所谓“最佳实践”删除、概括、合并、改名或擅自隐藏任何原始信息。
- 每个页面状态只使用一个全局主题，禁止浅色外壳与深色模块同时出现。
- 默认使用浅色主题。仅当用户明确要求深色，或输入数据显式包含深色模式按钮/激活状态时，才生成深色主题。
- 深色主题覆盖 Sidebar、Topbar、Page、Card、Table、Dialog 和专业工作区；优先映射输入数据提供的色号，不另造一套无关配色。
- 使用统一的深浅蓝品牌渐变表示主操作与高强调入口，使用可访问的中点纯蓝表示链接、焦点与图表主序列；使用青绿表示健康、在线和完成；只在实际风险上使用黄、橙、红。
- 拉丁文字与数字默认使用 Plus Jakarta Sans 400/500/600/700，中文按字形回退 PingFang SC / Microsoft YaHei；不混用另一套展示字体，也不使用浏览器伪造字重。
- 将信息层级建立在字号、字重、留白、边界和对齐上，不依赖大面积颜色和浓重阴影。
- 使用 8pt 网格、稳定的左侧导航、统一顶栏、统一卡片半径、统一状态语义。
- 保持数据密度：优先紧凑分组、可扫读表格和小型图表，不用巨型标题和过度空白稀释业务信息。
- 图表先服务问题再服务装饰：趋势、比较、构成、进度、排期、流程分别使用对应图形；不使用渐变、平滑曲线或多色去暗示数据中不存在的层级、连续性或风险。
- 让 AI 成为内联的工作助手：在对应业务模块内给出建议、证据、风险级别和操作入口，并服从当前全局主题，不做孤立的异色“AI 炫技区”。

## 内容保真与结构保护

当用户提供已有界面或内容作为改版输入时，先完成内容盘点，再开始设计：

1. 逐项记录原始页面中的导航、模块顺序、分组关系、父子层级、标题、说明、字段、数值、单位、状态、标签、表格列、Tab、按钮、操作入口和辅助信息。
2. 建立“原始信息 → 新界面位置”的一对一映射。每项信息必须在新界面中可见或通过明确、可发现的交互访问，不得丢失。
3. 锁定原有内容组织结构。保持模块顺序、信息分组、层级关系、业务语义、表格字段和导航关系不变；视觉样式不能成为改变结构的理由。
4. 允许优化交互承载形式，例如将平铺的次级操作改为下拉菜单、Popover、Dialog 或 Drawer，但必须保留全部原始文案、信息和功能，不改变业务含义，不增加不必要的访问步骤。
5. 响应式适配只能改变排列、折叠方式和容器形态；窄屏中收入菜单、抽屉或横向滚动的信息仍必须完整可达，禁止直接删列、删字段或删操作。
6. 只有用户明确授权精简、合并、改写或重组内容时，才可改变原有信息与结构，并在交付中列出变更项。

下拉菜单、Popover、Dialog、Drawer、Tooltip 等浮层必须服从当前全局主题，复用同一套色彩、字体、圆角、边框、阴影、图标、状态和动效 Token，不得出现与页面主体割裂的默认浏览器样式或另一套视觉语言。具体规则见 [references/design-system.md](references/design-system.md) 的“浮层与覆盖组件”。

## 工作流

### 1. 盘点原始内容并决定主题、任务与数据优先级

输入包含现有页面、截图、设计稿或代码时，先输出内部内容清单与结构树，确认每个原始信息项在新设计中的去向。没有完成一对一映射前，不开始视觉重构。

先按以下规则确定唯一主题，再开始布局：

1. 用户明确要求浅色：使用浅色。
2. 用户明确要求深色：使用深色。
3. 输入数据显式包含深色模式按钮或深色激活状态：使用深色；若数据同时提供对应色号，将其映射到语义 Token。
4. 输入仅表示“支持主题切换”，但没有当前状态或深色生成要求：仍默认浅色。
5. 没有任何主题信号：使用浅色。

如果用户或数据明确要求可切换主题，可实现主题切换，但任一时刻只渲染一套主题；不得并排展示，也不得在同一状态混合浅色卡片与深色工作区。

先列出当前用户的核心任务、高频操作、高风险信息与次要信息。不要因为视觉对称而平分不同优先级的内容。

### 2. 选择页面模板

从以下模板中选择最接近的骨架，仅替换业务内容，不随意改变全局布局：

- 经营总览 / Clinical Dashboard
- 对象详情 / Medical Records
- 时间调度 / Appointments & Scheduling
- 高密度队列 / Laboratory & Diagnostics
- 人员资源 / Doctors & Staff
- 专业工作区 / Patient Digital Twin（服从全局主题）
- 标准列表与设置页

若页面包含数据可视化，同时在 [references/chart-design-system.md](references/chart-design-system.md) 中完成“业务问题 → 图表族 → 编码方式”的选择。一个模块只回答一个主问题；同一仪表盘优先复用 2–3 个图表族，不为了丰富感堆满不同样式。

### 3. 建立共享应用外壳

先完成 Sidebar、Topbar、Page Header、内容容器和响应式断点，再填充页面。使所有页面复用同一套导航项、标题结构、搜索、通知、用户入口和页面边距。

### 4. 按固定顺序组装页面

使用下列默认顺序：

1. 页面标题、面包屑、页级操作。
2. 3–4 个关键指标卡。
3. 主任务工作区。
4. 例外、AI 洞察或风险提醒。
5. 补充图表、队列、日程或详情。

当页面主任务是处理队列时，让筛选器和表格提前，不要强行堆叠图表。

### 5. 用语义状态表达紧急性

同一状态在所有页面使用同一颜色、文案和形状。不要为每个模块发明新标签。标签保持低饱和语义底与高对比文字；强色底仅供主按钮或极端警报使用。

图表中的颜色分为两类：业务状态色与普通序列色。状态色必须沿用全局语义；普通序列不得借用 Critical / Warning 制造装饰性差异。所有可视化同时使用文字、数值、标记形状或排序提供非颜色线索。

### 6. 实现与验证

- 优先继承现有项目技术栈和组件库。
- 落地前先确认 Plus Jakarta Sans 400/500/600/700 已通过项目字体管线或本地 `@font-face` 实际加载；中文字符使用 PingFang SC / Microsoft YaHei 回退，不接受只声明字体名或伪造字重。
- 新项目默认使用语义化 HTML、CSS 变量和可复用组件；需要图标时使用 Lucide，并按图标单独导入，不使用 Emoji、Unicode 符号、图标字体或手工 SVG 图标。
- 严格区分图标盒与图标本体：KPI 图标盒使用 36–40px，内部 Lucide 图标使用 16–18px；两者必须是独立元素与 class。不得把容器尺寸 class 直接赋给 SVG，也不得让 KPI 图标贴边、顶边或占满背景色块。完整规则与示例见 [references/design-system.md](references/design-system.md) 的“KPI 卡”。
- 使用 `data-theme="light|dark"` 或等价的全局主题入口管理语义 Token；默认值为 `light`。除非明确要求主题切换，不要同时输出两套可见 UI。
- 使用真实结构的模拟数据，避免每个值都是整数、每条曲线都向上或所有状态都正常。
- 图表实现必须包含标题、时间范围/数据范围、单位、图例或直接标注、Tooltip/键盘等价信息、loading/empty/error/no-result 状态与文本摘要；数值格式、小数位和单位必须与来源一致。
- 完成后在桌面宽屏、笔记本宽度和窄屏下进行视觉验收，修复溢出、挤压、对齐和对比度问题。

## 不可违反的一致性约束

- 不因追求新风格、留白、简洁、卡片化或响应式效果而删除、概括、合并、改名、重新分组或隐藏用户提供的原始信息。
- 不擅自改变原有模块顺序、父子层级、导航关系、表格列、字段含义和操作集合；交互形式可优化，但信息与功能必须完整保留。
- 不让下拉菜单、Popover、Dialog、Drawer 或 Tooltip 脱离全局主题、组件 Token 与状态语义。
- 不在单个页面临时改变全局主色、字体、圆角或阴影。
- 不改变品牌渐变的色标、方向或字体家族；如输入提供明确品牌规范，仅允许在全局 Token 层统一覆盖，并重新验证对比度。
- 不在浅色主题中插入独立深色卡片、深色 AI 区或深色专业工作区；也不在深色主题中插入白色业务卡片。
- 不把主题选择等同于业务风险；深浅主题由用户要求或输入数据决定，警告仍使用语义状态色表达。
- 不同时使用多套图标风格、多套图表配色或多套卡片边框。
- 不把有缺口的数据画成连续平滑趋势，不把非构成数据画成堆叠/环图，不把普通类别涂成风险色，不使用双轴掩盖不同单位的不可比性。
- 不使用玻璃拟态、霓虹外发光、粗黑边框、糖果渐变、过度圆角或浮夸 3D 图标。
- 不把正常页面标题做成宣传页 Hero；后台内的标题必须紧凑、实用。
- 不仅以颜色表达状态；同时提供文字、图标、数值或形状线索。

## 输出要求

在交付中说明：

- 原始内容与新界面的一对一映射是否全部完成，是否存在经用户授权的结构或信息变更。
- 选用了哪一类页面模板。
- 选用了浅色还是深色主题，以及触发依据和色号来源。
- 哪些是全局共享组件，哪些是业务专属组件。
- 主要状态语义与数据优先级。
- 采用了哪些图表族、各自回答什么业务问题，以及是否存在阈值、缺失值或单位限制。
- 已完成的响应式、可访问性和视觉一致性检查。
